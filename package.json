{
  "name": "myfitnesscoach",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start -w @myfitnesscoach/mobile",
    "api": "npm run dev -w @myfitnesscoach/api",
    "build": "npm run build -w @myfitnesscoach/contracts && npm run build -w @myfitnesscoach/api",
    "typecheck": "npm run build -w @myfitnesscoach/contracts && npm run typecheck --workspaces --if-present",
    "test": "npm run test -w @myfitnesscoach/api",
    "format": "prettier --write apps packages docs README.md package.json .github"
  },
  "engines": {
    "node": ">=22.20.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  },
  "overrides": {
    "react": "19.2.3",
    "multer": "2.3.0",
    "xcode": {
      "uuid": "11.1.1"
    }
  }
}
